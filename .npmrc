; Anula legacy-peer-deps del .npmrc global del desarrollador:
; con legacy-peer-deps el lockfile se genera sin las dependencias peer
; y `npm ci` falla en cualquier servidor (Dokploy, CI, etc.)
legacy-peer-deps=false
