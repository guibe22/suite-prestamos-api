import { logger } from '../config/logger.js';

export const startNotificationWorker = () => {
  logger.info('⚙️ Notification Worker iniciado (stub)...');
  // Lógica para el envío de notificaciones (push, email, SMS, etc.)
};
